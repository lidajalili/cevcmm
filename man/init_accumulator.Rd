% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sufficient_stats.R
\name{init_accumulator}
\alias{init_accumulator}
\title{Initialize an empty sufficient-statistics accumulator}
\usage{
init_accumulator(p, q)
}
\arguments{
\item{p}{Integer. Number of fixed-effects columns (intercept plus spline
basis).}

\item{q}{Integer. Number of random-effects columns (length of alpha).}
}
\value{
A list of class \code{"vcmm_accumulator"} with the same six
matrix slots as \code{compute_sufficient_stats()}, all initialised to
zero, plus \code{n_obs = 0L}.
}
\description{
Allocates a zero-filled accumulator with the correct dimensions to receive
batched calls to \code{accumulate_stats()}. Use this once before a
streaming loop over data batches or nodes.
}
\examples{
acc <- init_accumulator(p = 5, q = 3)
dim(acc$C)   # 5 x 5
dim(acc$ZtZ) # 3 x 3
acc$n_obs    # 0
}
\seealso{
Other sufficient statistics:
\code{\link[=accumulate_stats]{accumulate_stats()}},
\code{\link[=compute_sufficient_stats]{compute_sufficient_stats()}}
}
\concept{sufficient statistics}
