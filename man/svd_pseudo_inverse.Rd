% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/svd_stable.R
\name{svd_pseudo_inverse}
\alias{svd_pseudo_inverse}
\title{SVD-based Moore-Penrose pseudo-inverse}
\usage{
svd_pseudo_inverse(A, use_split_merge = TRUE, verbose = FALSE)
}
\arguments{
\item{A}{Numeric square matrix.}

\item{use_split_merge}{Logical. If \code{TRUE} and \code{nrow(A) >= 100},
use \code{split_merge_svd_row()}; otherwise use base \code{svd()}.}

\item{verbose}{Logical. If \code{TRUE}, print method and condition number.}
}
\value{
A list with elements \code{inverse} (the pseudo-inverse matrix),
\code{condition_number}, \code{effective_rank}, and \code{method}
(character: \code{"standard SVD"} or \code{"split-merge SVD"}).
}
\description{
Computes \eqn{A^{+}} via SVD, optionally using the split-merge variant
for large matrices. Singular values below
\code{.Machine$double.eps * d[1] * max(dim(A))} are treated as zero.
}
\details{
This is an internal helper for \code{invert_matrix()}; end users should
call \code{invert_matrix()} instead.
}
\keyword{internal}
