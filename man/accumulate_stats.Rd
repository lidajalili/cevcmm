% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sufficient_stats.R
\name{accumulate_stats}
\alias{accumulate_stats}
\title{Add one batch or node statistics to a running accumulator}
\usage{
accumulate_stats(acc, stats)
}
\arguments{
\item{acc}{A \code{vcmm_accumulator} object from
\code{init_accumulator()}.}

\item{stats}{A \code{vcmm_ss} object from
\code{compute_sufficient_stats()}.}
}
\value{
The updated accumulator (class \code{"vcmm_accumulator"}).
}
\description{
Performs the additive aggregation
\code{acc <- acc + stats} component by component. After processing all
batches, the accumulator holds the full-data sufficient summary.
}
\examples{
set.seed(1)
n_batch <- 50; p <- 3; q <- 2
acc <- init_accumulator(p, q)

for (b in 1:3) {
  X <- cbind(1, matrix(rnorm(n_batch * (p - 1)), n_batch, p - 1))
  Z <- matrix(rnorm(n_batch * q), n_batch, q)
  y <- rnorm(n_batch)
  ss <- compute_sufficient_stats(y, X, Z)
  acc <- accumulate_stats(acc, ss)
}

acc$n_obs  # 150
}
\seealso{
Other sufficient statistics:
\code{\link[=compute_sufficient_stats]{compute_sufficient_stats()}},
\code{\link[=init_accumulator]{init_accumulator()}}
}
\concept{sufficient statistics}
